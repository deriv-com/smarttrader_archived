// TODO complete the functionality + add tests + complete the ui
const NetworkStatus = () => {
    return (
        <div>
            <p>NetworkStatus</p>
        </div>
    );
};

export default NetworkStatus;
